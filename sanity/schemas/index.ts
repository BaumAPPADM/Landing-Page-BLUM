import { landing } from './landing';
import {
  richText, navLink, stage, resultado, screen, plan,
  partnerLogo, stat, startCard, footerColumn,
} from './objects';

export const schemaTypes = [
  landing,
  richText, navLink, stage, resultado, screen, plan,
  partnerLogo, stat, startCard, footerColumn,
];
